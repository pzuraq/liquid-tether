<div class="example-button-container">
  <button type="button" {{on "click" this.openModalDialog}}>
    Open Dialog
  </button>
</div>

{{#if this.showFirstModalDialog}}
  <LiquidTether @to="modal-dialog" @target="document.body" @targetModifier="visible" @attachment="middle center" @tetherClass="modal-dialog" @overlayClass="modal-backdrop">
    <div class="modal-content">
      <div class="modal-header">
        <h4 class="modal-title">Modal Header</h4>
      </div>

      <div class="modal-body">
        <p>Here's a modal!</p>
      </div>

      <div class="modal-footer">
        <button type="button" {{on "click" this.closeModalDialog}}>Cancel</button>
        <button type="button" {{on "click" this.nextModalDialog}}>Next</button>
      </div>
    </div>
  </LiquidTether>
{{/if}}

{{#if this.showSecondModalDialog}}
  <LiquidTether @to="modal-dialog" @target="document.body" @targetModifier="visible" @attachment="middle center" @tetherClass="modal-dialog" @overlayClass="modal-backdrop">
    <div class="modal-content">
      <div class="modal-header">
        <h4 class="modal-title">Another Modal</h4>
      </div>

      <div class="modal-body">
        <p>
          This modal came in from the right instead of fading. The next modal
          will also slide in from the right, while the previous modal will slide
          in from the left, maintaing spacial context.
        </p>
      </div>

      <div class="modal-footer">
        <button type="button" {{on "click" this.prevModalDialog}}>Back</button>
        <button type="button" {{on "click" this.nextModalDialog}}>Next</button>
      </div>
    </div>
  </LiquidTether>
{{/if}}

{{#if this.showThirdModalDialog}}
  <LiquidTether @to="modal-dialog" @target="document.body" @targetModifier="visible" @attachment="middle center" @tetherClass="modal-dialog" @overlayClass="modal-backdrop">
    <div class="modal-content">
      <div class="modal-header">
        <h4 class="modal-title">Another Modal</h4>
      </div>

      <div class="modal-body">
        <p>
          This is the last modal! It'll fade out when you finish the dialog.
        </p>
      </div>

      <div class="modal-footer">
        <button type="button" {{on "click" this.prevModalDialog}}>Back</button>
        <button type="button" {{on "click" this.closeModalDialog}}>Finish</button>
      </div>
    </div>
  </LiquidTether>
{{/if}}
